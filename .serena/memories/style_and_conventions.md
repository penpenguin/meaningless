TypeScript project linted by ESLint with the recommended + @typescript-eslint configs. Unused vars are errors unless prefixed with _. Code is module-based (type: module) and uses TailwindCSS/DaisyUI utilities for styling plus GLSL shaders via vite-plugin-glsl. Tests use Vitest. Follow standard TS module exports, prefer typed interfaces from src/types, and keep code compatible with modern browsers (es2021).