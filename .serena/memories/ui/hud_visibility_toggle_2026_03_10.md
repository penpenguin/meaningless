2026-03-10: Added persistent HUD visibility control. `GameProfile.preferences` now includes `hudVisible` defaulting to true in fresh saves and migrated saves. Added `SETTINGS/SET_HUD_VISIBILITY` in `src/game/types.ts` / `src/game/gameReducer.ts`. `createGameHudOverlay` now has a topbar `Hide HUD` button (`data-action="hide-hud"`) and a fixed `Show HUD` reveal tab (`data-action="show-hud"`) that remains visible when the main `.hud-rail` is hidden. Hidden state preserves the current HUD mode and restores it when reopened. CSS lives in `src/styles.css` with `.hud-reveal-tab` anchored top-right on desktop and bottom-right on narrower viewports. Coverage added in `src/game/gameSave.test.ts`, `src/game/createGameStore.test.ts`, `src/components/GameHudOverlay.test.ts`, and `src/__tests__/viewportLayout.test.ts`. Verified with `npm run test`, `npm run typecheck`, and `npm run lint`.