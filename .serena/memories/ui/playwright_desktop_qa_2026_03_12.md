Desktop Playwright QA run against local Vite app at http://127.0.0.1:5174/meaningless/. Verified with real mouse input that mode tabs Tank/Unlock/Layout/Progress/Settings switch panels correctly; Sound and Motion toggles flip and restore correctly; Hide HUD exposes Show HUD and restores previous active panel. Visual screenshots captured at /tmp/aq-desktop-final.jpg, /tmp/aq-settings-bug.jpg, /tmp/aq-hidden-desktop.jpg, /tmp/aq-small-desktop.jpg. Findings: (1) quality segmented control appears misaligned/incorrect under real clicks in Settings; clicking the High button resulted in Medium becoming active, while clicking Medium from Low had no effect. The relevant creation/subscription code is in src/components/GameHudOverlay.ts createSettingsPanel. (2) at 1280x720, page-level viewport metrics are clean (no document scroll), but .hud-panel-container internally overflows (clientHeight 546, scrollHeight 640), and the lower portion of the Tank panel is not visible in the initial view, so some actions require internal scrolling in a fixed-shell layout.