2026-03-10: Improved `AdvancedAquariumScene` substrate realism in `src/components/AdvancedScene.ts`. `createSubstrate` now names the base/top meshes, increases substrate thickness, shapes the top plane into a back-rising bed instead of a flat ripple-only surface, and adds a new textured front substrate plane (`tank-substrate-front`) so the sand reads as deeper from the front camera angle. The sand material now uses dedicated canvas normal and roughness textures from new `createSandNormalTexture` and `createSandRoughnessTexture` helpers, while `createSandTexture` was reworked to layer drift lines, denser grains, pebble patches, and stronger edge shading. Added regression tests in `src/components/AdvancedScene.test.ts` to assert the named substrate meshes, the presence of the front face, and the use of normal/roughness maps. Verified with `npm run test`, `npm run typecheck`, and `npm run lint`. Automated Playwright visual capture still stalled in this environment, so visual QA was only partially automatable.