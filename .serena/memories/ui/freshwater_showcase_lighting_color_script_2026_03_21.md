2026-03-21: Reworked `src/components/AdvancedScene.ts` toward a freshwater planted showcase look with TDD. `applyGradientBackground()` now derives the environment backdrop from theme water tint blended toward olive-green (`#5f7467`), reducing blue-box feel while keeping fog color equal to the active theme waterTint. `setupAdvancedLighting()` now uses brighter daylight-biased ambient/hemi/fill balances (ambient 0.44, hemi 1.02, sun 1.94, fill 0.66, rim 0.11). `createBackdropTexture()` and `createWallPanelTexture()` were shifted to charcoal/olive values and weaker panel glow. `createHeroLightingLayers()` broadened the top canopy wash (0.88w x 0.44h) while lowering hero ground/front stage fill intensities. `createCausticsLayers()` reduced floor/back caustic opacity to avoid white stage-light sand. `applyTankTheme()` now emphasizes a freshwater color script: darker olive-charcoal depth/shadow, paler beige ground glow, thinner wall panel response, stronger top surface sheen than front waterline, and slightly lighter hardscape occlusion to reduce black crush. Surface/canopy/near-surface/midwater textures were adjusted toward broad daylight sheen and breakup patches rather than thin white streaks. Regression coverage updated in `src/components/AdvancedScene.test.ts`, plus background-theme tests in `src/__tests__/applyTheme.test.ts` and `src/__tests__/gradientBackground.test.ts`. Verified with `npm run test`, `npm run typecheck`, `npm run build` (existing Vite chunk-size warning remains).