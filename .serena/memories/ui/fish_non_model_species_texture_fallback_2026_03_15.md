2026-03-15: Updated `src/components/DetailedFish.ts` non-model fallback behavior in `createFishMaterial()`. Runtime order is now species atlases first (`baseColorTextureId`, `normalTextureId`, `roughnessTextureId`, `alphaTextureId`), then legacy `patternTextureId`, and procedural `createEmergencyFishTexture()` only when no diffuse texture can be resolved at all. Added tests in `src/components/DetailedFish.test.ts` covering species-texture fallback, emergency-only path when species/legacy textures are absent, and legacy-pattern-before-emergency ordering. Verified with `npm run test`, `npm run typecheck`, and `npm run build`.