2026-03-21: Retuned `src/components/Aquascaping.ts` nature-showcase planting to v5. `natureShowcasePlantClusterDefinitions` now push more screen-space broadleaf mass into `left-mid-broadleaf` and `center-left-fern-mass`, while `right-mid-crypt` / `right-rear` were simplified to crypt-led accents without hygrophila, so the right side no longer reads as a reed bundle. `createHeroCanopy()` for nature-showcase now uses five localized masses: one reduced vallis backdrop on the left-rear, javafern-large + javafern-narrow + anubias-nana broadleaf masses on the left/center-left, and a low crypt accent on the right. `natureShowcaseHardscapePlantAnchors` increased from 22 to 28 with extra driftwood root/fork and rock crevice/junction anchors so java fern and anubias read as attached to wood/rock. Added a dedicated `createCryptRosettePlant()` fallback and switched `crypt-brown` to a new `rosette` silhouette family, so fallback crypts render as low radial rosettes instead of strap bundles. Also lifted the nature-showcase background tint floor for vallis, stem/hygro, and crypt. Verified with `npm run test -- src/components/Aquascaping.test.ts`, `npm run test`, and `npm run typecheck`.