2026-03-13: Continued the premium aquarium upgrade with two hardscape-focused layers. In `AquascapingSystem`, added `createHardscapeTransitionDetails()` and a TDD spec so the hero composition now includes a low substrate berm (`hardscape-transition-berm`) plus six textured transition pebbles (`hardscape-transition-pebble`) around the hero ridge/driftwood base; this bridges the rock/wood silhouette into the sand instead of leaving a hard cut. In `AdvancedAquariumScene`, added a new additive rear panel `tank-hero-rim-light` via `createHeroRimLightTexture()` and integrated it into motion + quality handling so medium/high quality keep a soft silhouette-separating rim light behind the hardscape while low disables it. Verified with full `npm run test` (143 passing), `npm run typecheck`, and `npm run build` (existing chunk-size warning only).