2026-03-16: Reworked fish motion away from pinball wall rebounds. `src/utils/Boids.ts` now uses `Boid.update(deltaTime)` / `BoidsSystem.update(deltaTime)` with dt-based velocity/position integration, predictive wall steering (`predictedPosition = position + velocity * lookAhead`), comfort vs hard bounds, bounded wall steering (`maxForce * hardBoundaryMultiplier`), and an X-axis cruise bias for wide tanks. `avoidWalls` now feeds boundary margin/lookAhead/weight/inward strength through `BoidsSystem.setBehaviorTuning`. `src/components/DetailedFish.ts` now forwards `deltaTime` to boids, seeds wander targets relative to tank bounds, converts curiosity/sudden-turn/gentle-turn randomness to rate-per-second checks, and keeps per-fish `smoothedQuaternions`/`previousVelocities` so rendered heading follows velocity by slerp instead of snapping. Added regression coverage in `src/utils/Boids.test.ts` and expanded `src/components/DetailedFish.test.ts` for dt forwarding, bounds-relative wander seeding, and heading smoothing.