2026-03-21: Updated `nature-showcase` hardscape to v4 in `src/components/Aquascaping.ts` with TDD. `createHeroDriftwood` now treats `driftwood-hero` as the main wood core when present, skips the full procedural trunk in that case, and adds only short `driftwood-trunk-extender` meshes so the composition reads as asset core + secondary branches + extenders instead of one long spear. Shifted the driftwood origin further into the left mound, reduced showcase fallback trunk length, shortened branch attachments, and tightened the rightward reach while preserving a readable upper-right fan. Reworked `createHeroDriftwoodSecondaryAssets` so bridge/counter-fork/uplift-fork remain distinct but less tank-spanning. Strengthened the left dark rock mound by moving/enlarging the hero rock, increasing left foundation cluster mass, and turning the front-left transition cluster into a true support-rock foundation. `createHeroRockRidge` was reduced, shifted left/down, and embedded as part of the mound rather than a standalone hero. Added regression coverage in `src/components/Aquascaping.test.ts` to lock the asset-core-first showcase wood composition. Verified with `npm run test -- src/components/Aquascaping.test.ts src/components/AdvancedScene.test.ts`, `npm run typecheck`, and `npm run build` (existing Vite chunk-size warning remains).