2026-03-21: Reworked `src/components/AdvancedScene.ts` toward a broader daylight freshwater showcase look with TDD. `setupAdvancedLighting()` now uses warmer daylight-biased values (ambient 0.47, hemi 1.08, sun 2.02, fill 0.62, bounce 0.46, rim 0.09), and `applyLightingQuality()` was updated to preserve that balance in standard quality. `createBackdropTexture()` and `createWallPanelTexture()` were shifted further from blue-green toward charcoal/olive/muted green-brown. `createWaterSurfaceHighlightTexture()` and waterline/surface materials were re-tuned to read as broad meniscus sheen rather than near-white streaks. `createHeroLightingLayers()` plus `applyTankTheme()` now keep canopy/surface shimmer stronger while reducing front-fill/ground-glow stage-lighting, lowering caustic and substrate-detail glare, and keeping depth/occlusion olive-brown so hardscape stays readable without black crush. Regression coverage in `src/components/AdvancedScene.test.ts` now locks daylight lighting ratios, backdrop/wall/surface palette expectations, and the stronger top-surface vs reduced stage-fill relationship. Verified with `npm run test -- src/components/AdvancedScene.test.ts`, `npm run test`, and `npm run typecheck`.