2026-03-11: Upgraded aquarium visuals across fish silhouettes, environment reflections, and tank boundary cues using TDD. Added failing tests in `src/components/DetailedFish.test.ts` for silhouette aspect-ratio differences between Tropical/Angelfish/Neon, in new `src/components/Environment.test.ts` for layered radial depth blooms + silhouette strokes in environment background/cubemap, and in `src/components/AdvancedScene.test.ts` for premium waterline rim and glass edge glints plus low-quality suppression. Production changes: `DetailedFish.ts` now gives each fish variant silhouette parameters (body length/height/thickness, tail, dorsal/ventral, pectoral proportions) and `createDetailedFishGeometry()` builds variant-specific body/tail/fin geometry instead of one shared shape scaled per variant. `Environment.ts` now uses deterministic layered linear/radial gradients and stroked vertical silhouettes in both `createEnvironmentCubeMap()` and `createGradientBackground()` so glass/water reflections read deeper than random speckles. `AdvancedScene.ts` now adds left/right glass edge highlight meshes and a `tank-waterline-front` additive rim, themes them in `applyTankTheme`, animates them in `updateTankWaterMotion`, and quality-gates them in `applyVisualQuality` without adding unused shaders. Verified with targeted vitest, full `npm run test`, `npm run typecheck`, and `npm run build` (build still emits existing `lottie-web` eval warning and chunk-size warnings).