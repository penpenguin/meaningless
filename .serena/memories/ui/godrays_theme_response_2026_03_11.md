2026-03-11: Extended god rays to respond to aquarium theme values via TDD. Added failing tests in `src/components/GodRays.test.ts` for exported `resolveGodRayThemeValues(theme)`, new `rayTint`/`scatterTint` shader uniforms, and `GodRaysEffect.applyTheme(theme)` updating bloom + tint state; also added an `AdvancedScene.test.ts` assertion that `AdvancedAquariumScene.applyTheme()` forwards the theme to `godRaysEffect`. Production changes: `src/components/GodRays.ts` now imports `Theme` + `defaultTheme`, defines `GodRayThemeValues`, computes ray/scatter tint plus opacity/scatter multipliers and bloom parameters from `waterTint`, `fogDensity`, `particleDensity`, `surfaceGlowStrength`, and `causticsStrength`, applies those values through a new `applyTheme()` method, and uses themed tint/multipliers inside `update()`. `src/components/AdvancedScene.ts` now applies the resolved scene theme immediately after creating `GodRaysEffect` and forwards later theme changes into it. Verified with `npm run test`, `npm run typecheck`, `npm run lint`, and `npm run build` (existing `three-core` chunk-size warning remains).