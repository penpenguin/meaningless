2026-03-20: Upgraded hero driftwood from a single enlarged core into a multi-piece hardscape. `src/assets/visualAssets.ts` now registers `driftwood-secondary-a/b/c` plus `driftwood-bark-ao` and `driftwood-bark-cavity-mask`. `src/components/Aquascaping.ts` now assembles 2-3 secondary driftwood GLBs around the `driftwood-hero` core with per-piece non-uniform scale, rotation, and bury amount, expands root-base/burial detail, and applies bark-aware AO plus cavity-mask shader tuning so cavities stay darker while exposed ridges lift slightly. Added new public assets under `public/assets/aquarium/` for bark AO, cavity mask, and three secondary driftwood GLBs. Updated tests in `src/assets/visualAssets.test.ts` and `src/components/Aquascaping.test.ts` to cover manifest entries, cavity shader installation, and multi-piece secondary-branch assembly. Verified with `npm run test` (260 passing), `npm run typecheck`, `npm run build`, and `npm run lint`. Playwright screenshot capture against the live dev server stalled under WebGL load, so visual QA remained indirect in this run.