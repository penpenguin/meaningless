2026-03-17: Upgraded Aquascaping support rocks to an asset-backed-first pipeline with TDD. `src/assets/visualAssets.ts` now registers `rock-support-a`, `rock-support-b`, `rock-support-c`, and `rock-pebble-cluster` GLBs. `src/components/Aquascaping.ts` now routes rock materials through preserve-first rock tuning (`tuneRockMaterial` / `createRockReplacementMaterial`) so authored rock maps/colors are preserved, shared rock textures only fill missing channels, rock albedo is lifted away from black crush, `metalness` stays low, and `clearcoat` is clamped near zero. `createRocks()` was rebuilt from random primitive scatter into explicit placed rock groups: left/right visible support rocks prefer cloned GLB assets, each paired with a pebble cluster asset, and fallback support rocks are multi-mesh deformed clusters with subdivided polyhedra, shear/chip/asymmetry, and slightly buried bases rather than raw detail-0 primitives. Added regression coverage in `src/components/Aquascaping.test.ts` for rock detail-map gloss clamp, preserve-first authored support rock materials, asset-backed support placement, and clustered fallback behavior; added manifest coverage in `src/assets/visualAssets.test.ts`. Added `scripts/generate-support-rock-assets.mjs` and generated `public/assets/aquarium/rock-support-a.glb`, `rock-support-b.glb`, `rock-support-c.glb`, and `rock-pebble-cluster.glb` with embedded authored PBR textures. Verification passed with `npm run test`, `npm run typecheck`, and `npm run build`.