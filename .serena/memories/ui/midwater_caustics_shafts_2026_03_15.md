2026-03-15: Extended `src/components/AdvancedScene.ts` caustics from floor/back-only into a three-part scheme with dedicated midwater shafts. Removed the old single `tank-light-midwater` overlay and introduced `midwaterLightMeshes` populated by `createCausticsLayers()` as three named meshes (`tank-caustics-midwater-1..3`) using a new top-heavy, lower-breakup `createMidwaterLightTexture()`. Quality behavior now keeps all three shafts on `standard` and only one visible shaft on `simple`, while preserving canopy/rim/ground glow as supporting layers. Added midwater hardscape occlusion planes (`tank-hardscape-occlusion-midwater-driftwood`, `...-ridge`) via `createHardscapeOcclusionLayers()` plus a `shaft` variant in `createHardscapeOcclusionTexture()` so driftwood/ridge can cut through midwater beams without adding bloom or AO. Updated theme application, motion animation, and quality visibility to work on `midwaterLightMeshes` arrays instead of a single mesh. Tests in `src/components/AdvancedScene.test.ts` now assert the three midwater shafts, simple/standard visibility split, preserved OutputPass/light-rig behavior, and presence of the new midwater occlusion cutouts. Verification passed: `npm run test`, `npm run lint`, `npm run typecheck`, `npm run build` (build still emits the existing `three-core` chunk-size warning).