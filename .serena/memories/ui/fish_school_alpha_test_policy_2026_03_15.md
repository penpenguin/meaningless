2026-03-15: Updated `src/components/DetailedFish.ts` so `createFishAssetMaterial()` resolves alpha maps in preserve-first order for both school and hero fish: authored `baseMaterial.alphaMap` first, then `variant.alphaTextureId` fallback. School fish no longer enable transparent blending solely because an alpha map exists; they preserve authored `transparent`, use `alphaTest` floor 0.05 when any alpha map is present, and keep InstancedMesh rendering. School material tuning is intentionally more subdued than hero (`roughness` 0.38, `clearcoat` floor 0.64, `envMapIntensity` clamped into 0.62-0.78). Added tests covering school fallback alpha, authored alpha preservation, hero fallback regression guard, and instanced school mesh retention.