2026-03-15: Reworked substrate terrain shaping in `src/components/AdvancedScene.ts` to avoid a flat groomed floor. Added shared hardscape/plant anchor exports in `src/components/Aquascaping.ts` (`plantClusterDefinitions`, `substrateHardscapeAnchors`, `substratePlantAnchors`) and made `sampleSubstrateHeight()` combine asymmetric rear berm, front scoop, side drift, front silhouette breakup, multi-frequency noise, hardscape sink/rim relief, and plant micro-relief. Increased substrate geometry density to 96x64 for the top plane and 72x28 for the front face, while keeping the existing PBR substrate maps, uv2, anisotropy, and detail overlays. Front face shaping now samples the same terrain profile as the top surface so the foreground cross-section reflects the top crest instead of staying nearly straight. Adjusted driftwood burial details and the hardscape transition berm to use the shared anchor positions. Added regression tests in `src/components/AdvancedScene.test.ts` for subdivision density, irregular foreground crest, and local sink/rim burial around hero hardscape. Verified with `npm run test -- AdvancedScene Aquascaping`, `npm run typecheck`, and `npm run build` (existing chunk-size warning remains).