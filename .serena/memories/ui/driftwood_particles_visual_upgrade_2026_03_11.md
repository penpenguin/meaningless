2026-03-11: Improved aquarium visuals via TDD across aquascaping composition and water-volume particles. Added failing tests in `src/components/Aquascaping.test.ts` for a `hero-driftwood` hardscape built from TubeGeometry branches with attached `epiphyte-cluster` groups / `epiphyte-leaf` meshes, in new `src/components/EnhancedParticles.test.ts` for layered `bubble-plumes` + `suspended-motes` particle systems and low-quality mote suppression, and in `src/components/AdvancedScene.test.ts` to require `setWaterQuality()` forwarding quality changes to the particle system. Production changes: `Aquascaping.ts` now adds a driftwood arch focal element with three curved branches and attached epiphyte leaf clusters that reuse fan-leaf geometry/material behavior for subtle sway. `EnhancedParticles.ts` was rebuilt from a single generic bubble cloud into localized bubble plume emitters plus a softer suspended mote layer with separate shader materials and a new `setQuality()` path. `AdvancedScene.ts` now applies current visual quality to particles at creation time and when quality changes. Verified with targeted vitest, full `npm run test`, `npm run typecheck`, `npm run lint`, and `npm run build` (build still reports the existing >500 kB chunk warning for `three-core`).