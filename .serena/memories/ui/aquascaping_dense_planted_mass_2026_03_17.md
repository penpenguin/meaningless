2026-03-17: Reworked `src/components/Aquascaping.ts` default planted layout away from translucent card-like foliage toward dense planted masses. `plantClusterDefinitions` now emphasize four rear/background anchors (left rear, center rear, right rear, plus rear filler), two midground anchors, and two low foreground anchors; `createSeaweed()` increased repeated cluster count from 18 to 24 and raises background/midground scaling. Procedural ribbon/sword/fan builders now create denser clusters (background ribbon 6 fronds, background sword 7 leaves, background fan 6 leaves; non-background sword 8, fan 7) with stronger z-offset/rotation variance so masses survive edge-on views. Added plant material policy helpers: repeated foliage uses alpha cutout (`transparent=false`, `opacity=1`, higher `alphaTest`) and near-zero transmission; hero canopy / epiphytes keep only very weak transmission. Asset-backed plant materials in `createAssetBackedMaterial` and `createReplacementAssetMaterial` now inherit the same policy and get darker olive/deep-green tints by layer/role instead of glassy leaf settings. `createHeroCanopy()` now builds five tall canopy groups, prioritizing repeated use of `plant-sword-cluster` and `plant-fan-cluster`; procedural fallback uses sword/fan rather than ribbon so rear masses remain dense without assets. `createFreshwaterAccents()` was thickened with darker foreground tufts and taller rear stem groups. `src/components/Aquascaping.test.ts` now asserts the left/center/right rear mass layout, denser procedural cluster counts, alpha-cutout plant materials, and hero asset material clamping. Full `npm run test`, `npm run typecheck`, and `npm run build` passed after nudging the right-midground plant anchor away from the hero rock so `AdvancedScene` substrate berm expectations remained green.