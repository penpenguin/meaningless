2026-03-20: Refreshed authored fish assets with richer facial/fin/roughness detail. Added Butterflyfish as a separate render archetype and variant (`SpeciesArchetype` now includes `Butterflyfish`; `src/content/fish/butterflyfish.ts` maps to it). `src/components/DetailedFish.ts` now includes a Butterflyfish variant with dedicated basecolor/normal/roughness/alpha texture ids plus school/hero GLBs, and keeps preserve-first material fallback order unchanged. `src/assets/visualAssets.ts` manifest now exposes `fish-butterflyfish-*` PNGs and GLBs. `scripts/generate-fish-assets.mjs` now exports deterministic atlas helpers used by tests, authors distinct Goldfish / Angelfish / Butterflyfish atlas detail (eyes, gill seams, fin rays, alpha membranes, roughness breakup), and regenerates corresponding public PNG/GLB assets. Added tests in `src/assets/fishAssetAuthoring.test.ts` for goldfish facial/fin detail and butterflyfish non-metallic separation, plus updated manifest/variant mapping tests. Verification passed with `npm run test`, `npm run typecheck`, and `npm run build`.