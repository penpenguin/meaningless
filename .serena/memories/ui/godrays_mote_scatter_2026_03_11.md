2026-03-11: Follow-up visual upgrade added underwater particulate scatter to god rays via TDD. Added new `src/components/GodRays.test.ts` covering exported `GodRaysShader` scatter uniforms (`time`, `scatterStrength`, `scatterDrift`), `resolveSuspendedMoteScatter(scene)` behavior based on the `suspended-motes` layer, and `GodRaysEffect.update()` pushing mote-derived scatter values into shader uniforms. Production changes in `src/components/GodRays.ts`: exported `GodRaysShader`, added `resolveSuspendedMoteScatter()` plus hierarchy visibility handling, extended the fragment shader with drifting cell-noise sparkle / veil modulation inside light shafts, and updated `setUnderwaterParameters()` / `update()` so god rays react to actual mote visibility and density. Verified with `npm run test`, `npm run typecheck`, `npm run lint`, and `npm run build` (existing `three-core` chunk-size warning remains).