2026-03-20: Added a formal `nature-showcase` aquascape layout with TDD. `src/types/aquarium.ts`, `src/utils/stateSchema.ts`, and `src/utils/tankSchema.ts` now accept the new layoutStyle. `src/components/Aquascaping.ts` adds nature-showcase-specific plant placements, substrate anchor resolvers, left-heavy rock mound construction, left-anchored driftwood fan staging, integrated hero ridge placement, left-biased canopy/accent planting, curved hardscape transition details, and right-front-focused sand ripples. `src/components/AdvancedScene.ts` now makes substrate shaping and hardscape occlusion layout-aware via `resolveSubstrateHardscapeAnchors` / `resolveSubstratePlantAnchors`, with a custom left-mound/right-beach terrain profile for nature-showcase while keeping planted behavior unchanged. `src/assets/visualAssets.ts` now registers `rock-lava-base-cluster-a`, `rock-lava-base-cluster-b`, and `rock-lava-transition-chips`; corresponding GLBs were added by duplicating existing authored rock assets for immediate use. Regression coverage was added in `src/components/Aquascaping.test.ts`, `src/components/AdvancedScene.test.ts`, and `src/assets/visualAssets.test.ts` to lock the left hardscape mass, open sand beach, driftwood fan silhouette, and manifest entries. Verified with `npm run test`, `npm run typecheck`, and `npm run build` (existing Vite chunk-size warning remains).