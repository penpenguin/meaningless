2026-03-13: Refined photo mode to follow the premium hero fish layer. `DetailedFishSystem` now exposes `getHeroFocusPoint()` which returns the first visible hero fish position. `AdvancedAquariumScene` now resolves photo mode camera targets through a new helper that blends the base photo target toward the hero fish focus point, so `Photo Mode` tracks the foreground subject instead of always staring at a static center point. Added TDD coverage in `DetailedFish.test.ts` for hero focus selection and in `AdvancedScene.test.ts` for photo mode target biasing toward hero fish. Verified with `npm run test`, `npm run typecheck`, and `npm run build`.