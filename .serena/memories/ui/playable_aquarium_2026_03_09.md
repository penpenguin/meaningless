2026-03-09: Made the aquarium starter state visibly playable. Reduced renderModel fog density to a gentle range (`0.018 + (100-quality)/1800`) so healthy tanks remain readable. Replaced AdvancedScene substrate from circular CylinderGeometry mounds to a rectangular BoxGeometry base plus PlaneGeometry sand surface that matches the tank footprint. Improved GameHudOverlay with active mode button state (`aria-pressed` + `.is-active`), a mode-aware guidance card explaining the next step, and visible maintenance cost text (`Restore Water (N coins)`). Added regression tests in `src/game/renderModel.test.ts`, `src/components/AdvancedScene.test.ts`, and `src/components/GameHudOverlay.test.ts`. Full verification passed: `npm run test`, `npm run typecheck`, `npm run lint`. Narrow viewport QA confirmed no page-level vertical scroll and the HUD still fit within a 390x844 viewport.