2026-03-21: Retuned `nature-showcase` hardscape to v5 in `src/components/Aquascaping.ts` with TDD. Shifted `createHeroDriftwood()` further into the left mound, reduced showcase driftwood group x/y scale, and shortened the fallback procedural trunk so it no longer reads as a tank-spanning spear. In `fitHeroDriftwoodAssetCore()` reduced showcase target size and moved the asset core left/down so `driftwood-hero` acts as a mound-rooted core rather than a full-width dominant branch. Reworked `createHeroDriftwoodAssetExtenders()`, `createHeroDriftwoodSecondaryAssets()`, showcase branch attachments, and fine twigs so the composition reads as asset core + buried bridge + center-left counter-fork + small uplift/short extenders instead of a long diagonal single branch. Enlarged the showcase root flare slightly to reinforce the left mound origin. Moved `createHeroRockRidge()` further left/down and reduced ridge scale so the ridge reads as embedded mound support, not a standalone center-right hero. Updated `src/components/Aquascaping.test.ts` to lock shorter showcase right reach, center-left secondary branch placement, embedded ridge behavior, and preserved open right-front beach. Verified with `npm run test -- src/components/Aquascaping.test.ts`, `npm run test`, and `npm run typecheck`.