2026-03-19: Expanded fish locomotion differentiation in `src/components/DetailedFish.ts` with stronger per-variant profiles and stricter per-fish retarget timing. `LOCOMOTION_PROFILES` now push clearer species contrast: `disk-glider` has slower yaw, lower tail beat, longer glide/hover bias; `slender-darter` is faster with tighter tail cadence and burst-heavy short states; `goldfish-wobble` has more body wobble, delayed turn start, and stronger depth bob; `calm-cruiser` is steadier with lower curiosity and wider wall arcs. `scheduleNextRetargetTime` now clamps gait-specific cadence into a 4-14 second window, `chooseNextGaitState` suppresses same-state repeats more strongly, and `initializeRandomness` spreads cadence/speed multipliers further so schools do not share one rhythm.

Also strengthened motion rendering: school fish shader tail motion now adds a separate rear-body sway wave driven by per-instance phase/amplitude/frequency; hero fish now create a fallback `HeroTailPivot` rig when authored assets have no tail node so tail-driven motion still contributes visually without using root yaw. `applyHeroLocalMotion` restores stored local rig transforms each frame and applies different tail behavior for authored tail nodes vs fallback pivots.

Updated tests in `src/components/DetailedFish.test.ts` to lock the stronger variant deltas, require the single-mesh hero fallback tail pivot, and verify gait-specific retarget cadence stays within 4-14 seconds. Verified with `npm test`, `npm run typecheck`, and `npm run lint`.