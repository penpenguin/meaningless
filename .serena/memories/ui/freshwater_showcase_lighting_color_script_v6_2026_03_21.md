2026-03-21: Retuned `src/components/AdvancedScene.ts` and `src/components/DetailedFish.ts` for freshwater showcase color/light v6 with TDD. `setupAdvancedLighting()` and `applyLightingQuality()` now push a slightly broader daylight balance (ambient 0.53, hemi 1.18, sun 2.16, fill 0.56, bounce 0.46, rim 0.074 standard) while preserving tank-relative anchors. `createBackdropTexture()`, `createWallPanelTexture()`, `createDepthLayers()`, and the surface/canopy/midwater textures were shifted further from blue-green into charcoal, olive, and muted green-brown with warmer daylight-beige shimmer. `applyTankTheme()` now uses a stronger olive-brown freshwater tint, raises waterline highlight opacity slightly, further suppresses nature-showcase front-sand/caustic stage lighting (floor 0.04, back 0.027 multipliers), and keeps dark mound readability without black crush. `DetailedFishSystem.resolveFishMaterialResponse()` further subdues `nature-showcase` Goldfish and Butterflyfish by increasing roughness and lowering clearcoat/reflectivity/envMapIntensity so front/specular highlights do not clip into silver-plate glare. Regression coverage updated in `src/components/AdvancedScene.test.ts` and `src/components/DetailedFish.test.ts`. Verified with `npm run test -- src/components/AdvancedScene.test.ts`, `npm run test -- src/components/DetailedFish.test.ts`, `npm run test`, `npm run typecheck`, and `npm run build`.