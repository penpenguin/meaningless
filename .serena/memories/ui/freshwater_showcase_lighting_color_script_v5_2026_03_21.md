2026-03-21: Retuned `src/components/AdvancedScene.ts` to freshwater showcase color/light v5 with TDD. `setupAdvancedLighting()` now pushes a broader daylight balance (ambient 0.50, hemi 1.12, sun 2.08, fill 0.58, bounce 0.44, rim 0.078) while keeping the same tank-relative rig. `createBackdropTexture()` and `createWallPanelTexture()` were shifted further from blue-green into charcoal/olive/muted green-brown, and `createDepthLayers()` was nudged to olive-brown mid/deep values. `createHeroLightingLayers()` and `createUnderwaterLightingBands()` now favor a wider top canopy and slightly stronger near-surface daylight shimmer, while `createWaterSurfaceHighlightTexture()` was warmed and broadened for meniscus sheen instead of cool streaks. `createCausticsTexture()` was softened and warmed toward pale beige / muted olive. In `applyTankTheme()`, freshwater tinting now uses more olive-brown depth/shadow colors, stronger surface/waterline highlight relationship, lower wall-panel glare, lighter-but-not-crushed hardscape occlusion, and further reduced `nature-showcase` front-sand/caustic stage lighting. Regression coverage in `src/components/AdvancedScene.test.ts` now locks the daylight light-rig thresholds, backdrop/wall palette values, broad surface-shimmer stop colors, and subdued nature-showcase beach glare. Verified with `npm run test -- src/components/AdvancedScene.test.ts`, `npm run test`, and `npm run typecheck`.