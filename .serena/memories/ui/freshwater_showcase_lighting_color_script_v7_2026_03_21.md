2026-03-21: Retuned freshwater showcase light/highlight/specular in `src/components/AdvancedScene.ts` and `src/components/DetailedFish.ts` with TDD. `resolveToneMappingExposure()` now uses 1.40 standard / 1.29 simple. Main light rig remains daylight but slightly calmer and warmer: ambient `0xe3dfd0 @ 0.52`, hemi `0xf2ecdb / 0x4a4938 @ 1.15`, sun `0xfff0dc @ 2.14`, fill `0xd8d4c3 @ 0.52`, bounce `0x9c8d6d @ 0.43`, rim `0xd7d2c1 @ 0.068` with reduced simple-quality values. `createWaterSurfaceHighlightTexture()` and `createWallPanelTexture()` were shifted a little warmer/darker to remove cool-white/cyan feel. `applyTankTheme()` further subdues `nature-showcase` beach lighting by lowering hero ground glow/front fill, substrate detail/front detail, and caustic opacity floors. `DetailedFishSystem.resolveFishMaterialResponse()` further subdues showcase Goldfish and Butterflyfish by increasing roughness and reducing clearcoat/reflectivity/envMapIntensity so white fish highlights do not clip into silver-plate glare. Regression tests tightened in `src/components/AdvancedScene.test.ts` and `src/components/DetailedFish.test.ts`. Verified with targeted tests, `npm run test`, `npm run typecheck`, and `npm run build` (existing Vite chunk-size warning only).