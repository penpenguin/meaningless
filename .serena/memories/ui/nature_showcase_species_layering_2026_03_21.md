2026-03-21: Reworked `src/components/Aquascaping.ts` nature-showcase planting away from a vallisneria curtain toward species-layered colonies. `natureShowcasePlantClusterDefinitions` now use a stem/hygrophila-led `left-rear` background with vallisneria reduced to a minor backdrop species; `front-center` was moved to midground and converted into a broadleaf hardscape colony anchored by `anubias-nana-clump` with java fern satellites; `left-foot` remains the only foreground zone so the right-front beach stays open. Added a `driftwood-backfill` midground zone for irregular broadleaf transition around the wood base. `natureShowcaseHardscapePlantAnchors` now include extra driftwood fork pockets and wood-rock junction anchors so java fern/anubias read as attached epiphytes rather than substrate clumps. Added TDD coverage in `src/components/Aquascaping.test.ts` for reduced vallis wall behavior, presence of hygrophila/stem rear colonies, broadleaf center hardscape mix, and 10+ fork/junction hardscape anchors. Verified with `npm run test`, `npm run typecheck`, and `npm run build`.