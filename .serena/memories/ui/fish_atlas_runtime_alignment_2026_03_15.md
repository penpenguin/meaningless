2026-03-15: Aligned fish material routing with atlas-first runtime in `DetailedFish` and `visualAssets`. `FishVariant` now carries both atlas ids (`baseColorTextureId`, `normalTextureId`, `roughnessTextureId`, `alphaTextureId`) and legacy `patternTextureId` only for fallback paths. `createFishAssetMaterial()` now resolves in preserve-first order: authored `map` -> species atlas -> legacy pattern fallback; authored `alphaMap` -> species alpha; authored/species normal+roughness -> generic `fish-scale-normal`/`fish-scale-roughness` fallback. School fish keep authored `transparent` and use alpha fallback with `alphaTest`, avoiding forced blending. `createFishMaterial()` no longer uses atlas textures directly; it uses legacy `patternTextureId` if available, otherwise procedural canvas fallback. `visualAssets` manifest now exposes `fish-scale-normal` and `fish-scale-roughness` alongside per-species atlas ids, while fish diffuse SVG ids remain excluded. Verified with `npm run test -- --run src/components/DetailedFish.test.ts src/assets/visualAssets.test.ts`, `npm run test`, `npm run typecheck`, and `npm run build`.