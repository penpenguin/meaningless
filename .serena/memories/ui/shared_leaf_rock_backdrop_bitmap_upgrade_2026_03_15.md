2026-03-15: Lowered the shared SVG ceiling for large-area aquarium assets while keeping stable material ids. `src/assets/visualAssets.ts` now points `leaf-diffuse/alpha/normal/roughness`, `rock-diffuse/normal/roughness`, and `backdrop-depth` at PNG files instead of SVG, preserving existing ids and `VisualAssetBundle` / fallback contracts. Added TDD coverage in `src/assets/visualAssets.test.ts` to lock the PNG paths and `BASE_URL` resolution for those ids. Added deterministic generator `scripts/generate-shared-aquarium-textures.mjs` that emits `leaf-*.png` and `rock-*.png` at 1024x1024 plus `backdrop-depth.png` at 2048x2048 into `public/assets/aquarium/`. The generator uses pure Node PNG encoding and seeded procedural detail so no external CLI is required. Shared fish-scale SVG fallback maps were intentionally left unchanged because they are not large-area shared assets. Verified with `npm run test`, `npm run typecheck`, and `npm run build`.