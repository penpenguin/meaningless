2026-03-12: Upgraded tank wall and substrate quality in `src/components/AdvancedScene.ts` via TDD. Added failing tests in `src/components/AdvancedScene.test.ts` for interior wall response panels (`tank-wall-back-panel`, `tank-wall-left-panel`, `tank-wall-right-panel`), a translucent substrate detail layer (`tank-substrate-detail`), and a front sediment overlay (`tank-substrate-front-detail`). Production changes: `createAdvancedTank()` now inserts interior wall panels driven by new `createWallPanelTexture()` so the back/side walls catch light instead of reading as empty glass planes. `createSubstrate()` now adds two new sediment layers above the top plane and front face using new `createSubstrateDetailTexture()` / `createSubstrateDetailAlphaTexture()`, with coarser repeat values so the floor reads as layered sediment rather than a flat sand sheet. Added state wiring so wall panels and both sediment overlays respond to theme tint, quality scaling, and subtle motion. Verified with `npm run test`, `npm run typecheck`, and `npm run build`. Desktop Playwright visual QA confirmed the wall panels read more clearly; image capture tooling became unstable for the final post-refinement floor screenshot, so the last floor-strengthening pass was validated by code/test results plus an earlier desktop screenshot of the same QA flow.