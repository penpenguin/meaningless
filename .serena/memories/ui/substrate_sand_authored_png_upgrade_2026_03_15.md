2026-03-15: Upgraded substrate sand fidelity in `src/assets/visualAssets.ts` and `src/components/AdvancedScene.ts`. The asset manifest now points `substrate-sand-albedo/normal/roughness/ao` to authored PNG files in `public/assets/aquarium/` while keeping the old SVG files in place as repo fallback artifacts. `AdvancedAquariumScene.createSubstrate()` still preserves existing tiling, anisotropy, uv2, and aoMap wiring, but now resolves authored sand textures first and falls back to procedural `createSandTexture/createSandNormalTexture/createSandRoughnessTexture/createSandAoTexture` when assets are missing. Added a new procedural AO helper so fallback still exercises the aoMap path. When authored albedo is present, sand top/front materials use a lighter tint and slightly stronger normal response so the new authored microdetail is not crushed by the old warm multiply. Tests were expanded in `src/assets/visualAssets.test.ts` and `src/components/AdvancedScene.test.ts` to cover PNG manifest URLs, BASE_URL resolution, missing-asset fallback, top/front authored map usage, and substrate detail behavior in both simple and standard quality. Verified with `npm run test`, `npm run typecheck`, and `npm run build`.