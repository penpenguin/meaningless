2026-03-17: Reduced visible rectangular bands in `src/components/AdvancedScene.ts` via TDD. Added `createFeatherMaskTexture()` and applied alpha maps to `tank-backdrop`, `tank-backdrop-overlay`, `tank-depth-midground`, and `tank-depth-foreground-shadow`. Overlay opacity was lowered from 0.44 to 0.32 while keeping additive blending. Midground / foreground textures were rebuilt with extra diffuse radial blooms plus `destination-in` edge fades to break the flat-plane look. Theme application now keeps depth layers softer (`depthMidgroundMesh` opacity formula lowered to `0.15 + surfaceGlowStrength * 0.12`, foreground shadow to `0.035 + glassReflectionStrength * 0.08`). Caustics were softened by lowering base opacities (`floor` 0.11 initial, theme multiplier 0.26; `back` 0.08 initial, theme multiplier 0.17) and shifting highlight colors away from near-white toward pale cyan / off-white; cluster alphas inside `createCausticsTexture()` were also reduced. Added tests in `src/components/AdvancedScene.test.ts` to require feather alpha maps on backdrop/depth layers, weaker caustics opacity, and `destination-in` masking in depth textures. Verification passed with `npm test -- src/components/AdvancedScene.test.ts`, `npm run typecheck`, `npm run lint`, and `npm run build` (existing Vite chunk-size warning remains).