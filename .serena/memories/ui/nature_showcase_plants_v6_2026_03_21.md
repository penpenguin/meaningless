2026-03-21: Updated `src/components/Aquascaping.ts` to nature-showcase plants v6 with TDD. Reduced the left-rear tall backdrop mass by lowering showcase vallisneria weight and canopy scale so the background no longer reads as a full long-leaf curtain. Expanded `left-mid-broadleaf` and `center-left-fern-mass` with more satellites/offshoots and heavier java fern/anubias mixes so broadleaf screen-space mass reads at the driftwood shoulder and center-left hardscape. Simplified `right-mid-crypt` and `right-rear` to crypt-only species mixes with smaller, lower masses so the right side reads as dedicated crypt accent instead of mixed strap foliage. Reworked `createHeroCanopy()` into six localized showcase canopies: one reduced vallis backdrop on the far left, two fern canopies, two anubias canopies, and one low crypt accent on the right. Expanded `natureShowcaseHardscapePlantAnchors` with extra driftwood root/fork and rock crevice/junction anchors so attached java fern/anubias clusters are materially denser. Retuned `createCryptRosettePlant()` toward a lower, wider rosette and lifted background plant tint/lightness floors in `getPlantTint()` / `tonePlantColor()` so background foliage avoids black-line collapse. Updated `src/components/Aquascaping.test.ts` to lock denser broadleaf zones, 40+ hardscape anchors, 6-canopy composition, and crypt-only right accents. Verified with `npm run test -- src/components/Aquascaping.test.ts`, `npm run test`, `npm run typecheck`, and `npm run build`.