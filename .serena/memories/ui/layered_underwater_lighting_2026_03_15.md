2026-03-15 (later update): Re-aligned `src/components/AdvancedScene.ts` and `src/components/AdvancedScene.test.ts` with the new underwater-lighting spec. `createAdvancedTank()` now creates hero layers plus `createUnderwaterLightingBands()`, which owns three near-surface additive bands named `tank-light-near-surface-band-0..2` and a single additive midwater scatter mesh `tank-light-midwater`. `createCausticsLayers()` now only creates `tank-caustics-floor` and `tank-caustics-back`; the old `tank-caustics-midwater-*` meshes were removed. `createHardscapeOcclusionLayers()` now keeps only the required NormalBlending occlusion meshes: `tank-hardscape-occlusion-driftwood`, `...-ridge`, and `...-backwall`; the old midwater occlusion meshes were removed. Quality behavior changed so `simple` preserves the underwater lighting structure (canopy, all three near-surface bands, midwater layer, rim light, ground glow, caustics, and occlusion stay present) but lowers opacity instead of hiding these layers. Verification passed: `npm run test`, `npm run typecheck`, `npm run lint`, `npm run build` (build still emits the existing large `three-core` chunk warning).