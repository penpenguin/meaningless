2026-03-15: Reworked underwater lighting in `src/components/AdvancedScene.ts` to reduce uniform full-screen overlays and tie light variation to aquarium space. Added depth-separated lighting layers: three near-surface shaft bands (`tank-light-near-surface-band-*`), one `tank-light-midwater`, and substrate/backwall pools via `tank-hero-ground-glow` plus `tank-caustics-floor` / `tank-caustics-back`. Added cheap hardscape-linked occlusion meshes (`tank-hardscape-occlusion-driftwood`, `tank-hardscape-occlusion-ridge`, `tank-hardscape-occlusion-backwall`) aligned from `substrateHardscapeAnchors` instead of adding new shadow-mapped lights. Quality behavior: `standard` keeps full layered shafts + midwater + canopy + hero rim/ground glow; `simple` keeps the scene slightly brighter but non-uniform by retaining two near-surface bands, caustic pools, and occlusion while hiding the extra shaft band, midwater sheet, canopy, and hero-only glow/rim layers. Lighting balance was shifted by slightly raising sun/hemi/fill/bounce and reducing rim intensity, with small exposure adjustments (`standard` 1.36, `simple` 1.27). Verification: `npm run test`, `npm run typecheck`, `npm run lint`, `npm run build` all passed; Vite still reports the existing large `three-core` chunk warning during build.