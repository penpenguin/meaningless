2026-06-08: Added the provided Amazon sword GLB as `public/assets/aquarium/models/plants/plant-amazon-sword.glb` and registered it in `src/assets/visualAssets.ts` as `plant-amazon-sword`. `src/components/Aquascaping.ts` now treats `amazon-sword` as a concrete `PlantType` with strap/sword fallback geometry and green plant material tuning. The nature-showcase left-rear colony uses Amazon sword as the core asset while preserving stem-green-bush, hygrophila-rear, and matsumo satellites/offshoots so the rear remains species-layered instead of becoming a single sword curtain. Tests updated in `visualAssets.test.ts` and `Aquascaping.test.ts`; verified with targeted Red/Green tests, full `npm run test`, `npm run typecheck`, `npm run build`, `npm run lint`, and a Playwright network/runtime smoke test confirming `/meaningless/assets/aquarium/models/plants/plant-amazon-sword.glb` returns 200 with no page errors.