2026-03-09: Added behavior-driven fish realism without changing GameSave schema. `createAquariumRenderModel` now derives `schoolMood` (`calm|alert|feeding`) plus `depthVariance` and `turnBias` from water quality, comfort, crowding, and lane density. `DetailedFishSystem` now resolves a weighted behavior profile across all fish groups and uses it to bias maxSpeed/maxForce, vertical depth-seeking, wander strength, float amplitude, and turn noise. `GameHudOverlay` now surfaces a `Behavior` card (`Settled`, `Surface active`, `Alert`) using render-model moods and rewrites guide copy toward observation/depth balancing. `simulation.ts` now penalizes lane imbalance only once total fish reaches 15+, and adds decor-driven `hideoutScore` (plant 1, coral 2, cave 4) so hideouts offset crowding more in packed tanks. Verified with `npm run test`, `npm run typecheck`, `npm run lint`.