2026-03-16: Made the shared aquarium tank layout decisively wider. `src/components/aquariumLayout.ts` now uses dimensions 19.8w x 12.4h x 11.4d (front aspect ~1.597, up from ~1.254), keeps open-water bounds derived from the shared dimensions + fixed margins, retunes fit-driven framing to ~84% default and ~86.8% photo width coverage at 16:9, and changes primary shadow frustum ratios to true tank-relative values (-0.71/0.71/0.8/-0.73). `src/components/AdvancedScene.ts` rebalanced hero lighting anchors for the wider frontage (canopy, near-surface bands, midwater, hero rim, ground glow) and moved back caustics/backwall occlusion farther off-center so the scene does not cluster visually in the middle after widening. `src/components/DetailedFish.ts` keeps mobile at 25 fish but raises the default desktop school to 66 and widens initial / refreshed wander spans in X while tightening Y/Z margins to emphasize lateral swimming without changing `Boids.ts`, since Boids already derives neighbor radius and wall comfort from bounds/aspect. Regression coverage was updated in `sceneBounds.test.ts`, `AdvancedScene.test.ts`, and `DetailedFish.test.ts`; full `npm test`, `npm run typecheck`, `npm run lint`, and `npm run build` pass (build still emits the existing >500 kB three-core chunk warning).