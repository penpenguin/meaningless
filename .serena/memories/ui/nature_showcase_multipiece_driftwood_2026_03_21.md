2026-03-21: Reworked `nature-showcase` driftwood in `src/components/Aquascaping.ts` to read as a multi-piece hardscape instead of a single dark branch. Tightened `createHeroDriftwood` left-mound placement, reduced the fitted `driftwood-hero` asset core so it acts as trunk core rather than full silhouette, and retuned `createHeroDriftwoodSecondaryAssets` so `driftwood-secondary-a` is a buried bridge/support, `-b` is a mid counter-fork, and `-c` is an exposed uplift fork. Increased bark readability in `tuneDriftwoodMaterial` and `installDriftwoodCavityShading` with stronger longitudinal normal scale, lighter bark floor, higher emissive floor, and cavity/ridge contrast. Expanded left root contact with a larger showcase-only root flare, four root bases, broader burial lip, and five detritus mounds. Added regression tests in `src/components/Aquascaping.test.ts` for role-separated secondary branches, stronger bark shading thresholds, and extra left-mound root contact. Verified with `npm run test`, `npm run typecheck`, and `npm run build`.