2026-03-15: Upgraded driftwood realism in Aquascaping with TDD. `src/assets/visualAssets.ts` now uses bark PNGs for driftwood (`driftwood-diffuse/normal/roughness.png`) and adds `driftwood-ao.png`. `Aquascaping.ts` now preserves authored driftwood `map/normalMap/roughnessMap/aoMap` on cloned GLB materials, supplements only missing maps from shared textures, clamps wood `envMapIntensity` to a low range, and ensures `uv2` is generated when AO is applied. Procedural fallback driftwood was rebuilt around deformed TubeGeometry branches/branchlets/roots with elliptical cross-sections, taper, twist, bark noise, broken stubs, a root flare, and added burial shadow/detritus mounds for substrate contact. Added tests in `src/assets/visualAssets.test.ts` and `src/components/Aquascaping.test.ts` covering PNG manifest entries, driftwood AO support, authored-material preservation, missing-map supplementation with uv2 generation, fallback irregularity, and burial details. Replaced `public/assets/aquarium/driftwood-hero.glb` with a heavier embedded-texture version and added new driftwood PNG maps in `public/assets/aquarium/`. Verified with `npm run test`, `npm run typecheck`, and `npm run build`; also confirmed dev-server HTTP 200 responses for driftwood GLB/PNG assets.