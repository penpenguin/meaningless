2026-03-21: Updated `src/components/AdvancedScene.ts` and lightly adjusted `src/components/Aquascaping.ts` for nature-showcase substrate transition v3 with TDD. In `sampleSubstrateHeight()` strengthened the left hardscape mound, narrowed and pushed the pale beach farther into the right-front corner, reduced transition lift in the center-right, and amplified asymmetric shoreline breakup so the central-right area no longer reads as a white oval pedestal. In `createSubstrate()` lowered nature-showcase sand brightness slightly and darkened/reduced opacity for top/front sediment detail layers so the front half reads less like a stage surface. In `applyTankTheme()` reduced nature-showcase hero ground glow, hero front fill, substrate detail/front-detail emissive+opacity, and floor/back caustic opacity to suppress stage-lighting over the beach. In `src/components/Aquascaping.ts` reduced showcase `sand-ripple` count, scale, and opacity so front sand accents stay secondary. Updated `src/components/AdvancedScene.test.ts` with stricter nature-showcase mound/beach/transition and subdued-caustics thresholds; existing `src/components/Aquascaping.test.ts` passed unchanged. Verified with `npm run test -- src/components/AdvancedScene.test.ts`, `npm run test -- src/components/Aquascaping.test.ts`, `npm run test`, `npm run typecheck`, and `npm run build`.