2026-03-12: Continued the visual upgrade by adding front-of-tank hero fish and a premium backdrop overlay. `DetailedFishSystem` now creates up to three dedicated `hero-fish` meshes mapped to selected boid indices, hides the instanced duplicate for those fish, enlarges/slightly offsets them toward the viewer, and only shows them on `high` quality. Hero fish materials clone the variant material and boost clearcoat/env/emissive response. `AdvancedAquariumScene` now layers an additive `tank-backdrop-overlay` mesh when the visual asset bundle contains `backdrop-depth`, giving the rear of the tank a richer silhouette layer without replacing the procedural base backdrop. Added `public/assets/aquarium/backdrop-depth.svg` and expanded tests in `DetailedFish.test.ts` and `AdvancedScene.test.ts`. Verified with full `npm run test`, `npm run typecheck`, and `npm run build`.