2026-03-20: Refined hero driftwood readability in `src/components/Aquascaping.ts` with TDD. Raised the bark lightness floor to 0.40 and minimum emissive intensity to 0.18 in `tuneDriftwoodMaterial`, reduced cavity darkening to 0.18 while increasing ridge lift to 0.13 in `installDriftwoodCavityShading`, lowered local shadow opacity to 0.08 and burial shadow opacity to 0.18, and strengthened driftwood-local fill/rim lights to 3.4 and 1.04 so the multi-piece hardscape reads less like a black silhouette. Updated `src/components/Aquascaping.test.ts` to lock stronger fill/rim thresholds, lower shadow opacities, higher bark brightness expectations, tighter cavity/ridge shader tuning, and explicit `bridge` / `counter-fork` / `uplift-fork` coverage for secondary branches. Verified with `npm run test -- src/components/Aquascaping.test.ts src/assets/visualAssets.test.ts`, `npm run typecheck`, `npm run lint`, and `npm run build`. Visual QA on the dev server showed the scape is still very dark at `Water 0`, but in clear water (`Water 100`) the trunk-plus-secondary-branch composition and bark relief read correctly from the front.