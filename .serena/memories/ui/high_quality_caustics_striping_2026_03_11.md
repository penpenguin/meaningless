2026-03-11: Fixed High-quality stripe artifact in `src/components/AdvancedScene.ts`. Root cause was `createCausticsTexture()` drawing repeated bezier stroke bands, which read as vertical stripes under High quality. Added a regression test in `src/components/AdvancedScene.test.ts` asserting caustics use multiple radial gradients/fills and no stroke calls. Replaced the texture generation with layered radial caustic clusters and core blooms drawn via `fillRect` + `createRadialGradient`, preserving repeat wrapping while avoiding banding. Verified with targeted `vitest`, full `npm run test`, `npm run typecheck`, `npm run lint`, and `npm run build` (existing `lottie-web` eval and `three` chunk warnings remain).