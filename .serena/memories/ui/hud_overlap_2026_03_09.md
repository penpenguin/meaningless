2026-03-09: Fixed HUD overlap caused by `.hud-topbar` and `.hud-panel-container` being independently absolutely positioned. Wrapped both inside a new `.hud-rail` flex column in `createGameHudOverlay`, switched the panel container to relative positioning, and let mobile breakpoints reposition the shared rail instead of using a hard-coded `top: 5.5rem` offset. Added regression coverage in `src/components/GameHudOverlay.test.ts` (rail structure) and `src/__tests__/viewportLayout.test.ts` (desktop rail CSS, no fixed top offset). Verification passed with `npm run test`, `npm run typecheck`, and `npm run lint`.