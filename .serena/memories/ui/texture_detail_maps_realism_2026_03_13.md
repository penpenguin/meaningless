2026-03-13: Improved aquarium texture realism via TDD. Added failing tests in `src/components/Aquascaping.test.ts` and `src/components/DetailedFish.test.ts` requiring external detail maps (leaf normal/roughness, driftwood normal, rock normal, fish scale normal/roughness). Production changes: expanded `aquariumAssetManifest` in `src/assets/visualAssets.ts` with new SVG assets (`leaf-normal`, `leaf-roughness`, `rock-normal`, `driftwood-normal`, `fish-scale-normal`, `fish-scale-roughness`), wired those maps into `AquascapingSystem` (`createLeafMaterial`, `createDriftwoodMaterial`, `createRockMaterial`) and `DetailedFishSystem/createFishMaterial`, and tuned normal scales / roughness for more readable surface relief. Refreshed diffuse + roughness SVGs for leaf, driftwood, rock, and all fish species to add more nuanced veins, grain, mineral breakup, and pattern variation. Verification: targeted vitest for aquascaping and fish premium-material paths, `src/assets/visualAssets.test.ts`, full `npm run test`, `npm run typecheck`, and `npm run build` all passed.