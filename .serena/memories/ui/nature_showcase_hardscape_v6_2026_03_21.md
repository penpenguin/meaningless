2026-03-21: Updated `src/components/Aquascaping.ts` to `nature-showcase` hardscape v6 with TDD. Shifted the showcase driftwood group further left/down and reduced scale so the silhouette starts from the left mound instead of dominating the tank diagonally. Shortened the fallback procedural trunk, reduced both showcase-only trunk extenders into a buried root-side helper plus a small tip extender, and shrank/repositioned the fitted `driftwood-hero` asset core so authored wood remains the main silhouette without a full-length procedural trunk layered over it. Retuned `createHeroDriftwoodSecondaryAssets()` so `driftwood-secondary-a` is a more buried left bridge/root support, `-b` sits nearer center-left as a counter-fork, and `-c` lifts from the center as a smaller uplift fork. Strengthened the left dark rock mound in `createRocks()` by moving the hero rock and all four left support clusters further into a connected left-to-center-left foundation with slightly larger lava-base/support elements and more burial. Embedded `createHeroRockRidge()` deeper into that mound by moving it left/down, reducing showcase ridge scale, and compressing fallback ridge pieces so the ridge reads as root support rather than a standalone center-right hero stone. Updated `src/components/Aquascaping.test.ts` to lock tighter right reach, stronger left mound foundation, and more center-left secondary wood placement. Verified with `npm run test -- src/components/Aquascaping.test.ts`, `npm run test`, `npm run typecheck`, and `npm run build`.