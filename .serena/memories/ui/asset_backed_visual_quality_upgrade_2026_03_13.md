2026-03-13: Upgraded aquarium visuals to asset-backed fish/hardscape/environment while preserving gameplay/store/save/HUD logic. `src/assets/visualAssets.ts` now resolves public URLs through `import.meta.env.BASE_URL`, preloads textures + GLB models + HDRI (`GLTFLoader`, `RGBELoader`), validates school models as single-mesh single-material, and returns `VisualAssetBundle` with `textures/models/environment`. Added repo-hosted assets in `public/assets/aquarium/`: school+hero fish GLBs for tropical/angelfish/neon/goldfish, plant sword/fan cluster GLBs, driftwood/rock hero GLBs, authored substrate sand SVG PBR maps, and `aquarium-hdri.hdr`. `DetailedFishSystem` variants now map archetypes to `schoolModelId`/`heroModelId`; schooling fish keep `InstancedMesh` and use loaded model geometry/material when available; hero fish use hero model geometry when available; missing assets fall back to procedural geometry/material; shared geometries skip disposal. `AquascapingSystem` can clone hero driftwood/rock/plant assets and reapply existing leaf/wood/rock texture maps to cloned materials; missing hero assets fall back to existing procedural generators. `EnvironmentLoader` now accepts renderer + optional visual assets, uses preloaded or lazily loaded HDRI with PMREM when possible, and falls back to the procedural cube map without touching `scene.background`. `AdvancedScene.createSubstrate()` keeps the height profile but prefers authored sand albedo/normal/roughness/ao maps, sets anisotropy, and adds `uv2` for AO while preserving existing glow/depth/caustics layers. Tests expanded in `visualAssets.test.ts`, `DetailedFish.test.ts`, `Aquascaping.test.ts`, `Environment.test.ts`, and `AdvancedScene.test.ts`. Verified with `npm run test`, `npm run typecheck`, and `npm run build`. Fallback points: texture/model/HDRI load failure -> null asset; school model validation failure -> procedural fish; hero hardscape/plant missing -> procedural aquascaping; HDRI or PMREM unavailable -> procedural environment cubemap; substrate maps missing -> existing procedural sand textures.