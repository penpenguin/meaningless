2026-03-10: Expanded `AquascapingSystem` plant variety beyond ribbon seaweed. Plant clusters now carry `plantType` metadata and mix three archetypes: `ribbon-seaweed`, `sword-leaf`, and `fan-leaf`. Added new shape-based geometries for sword/fan leaves plus a new `createLeafMaterial` for broader translucent foliage while preserving existing ribbon frond rendering. Strengthened `src/components/Aquascaping.test.ts` to assert all three plant types are present and that fan leaves use `ShapeGeometry`. Verified with `npm run test`, `npm run typecheck`, and `npm run lint`. Playwright page rendering loaded, but screenshot capture hung in this environment, so visual signoff remained manual-only.