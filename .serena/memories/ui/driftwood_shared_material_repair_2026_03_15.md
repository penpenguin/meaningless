2026-03-15: Repaired driftwood shared-material fallback path in `src/components/Aquascaping.ts` with TDD. Added/updated tests in `src/components/Aquascaping.test.ts` so driftwood fallback must be `MeshPhysicalMaterial`, keep `driftwood-ao`, clamp `envMapIntensity` to <= 0.14, and preserve authored base-color maps when replacing non-PBR authored materials. Implementation now centralizes wood tuning in `tuneDriftwoodMaterial()`, keeps authored `map/normalMap/roughnessMap/aoMap` when present, fills only missing maps from shared textures, and routes `createReplacementAssetMaterial()` plus procedural `createDriftwoodMaterial()` through a physical-material helper instead of generic `MeshStandardMaterial`. Verified with targeted Vitest for `visualAssets`, `Aquascaping`, and `AdvancedScene`, plus `npm run typecheck`.