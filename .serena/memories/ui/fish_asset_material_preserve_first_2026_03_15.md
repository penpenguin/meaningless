2026-03-15: Reworked fish visuals so asset-backed fish no longer depend on generic fish SVG diffuse textures. `src/assets/visualAssets.ts` now manifests per-species PNG atlases (`fish-<species>-basecolor/normal/roughness/alpha`) instead of `fish-*.svg` and shared fish scale maps. `src/components/DetailedFish.ts` now gives each variant species-specific texture ids, preserves authored GLB material channels first in `createFishAssetMaterial()`, only supplements missing channels from shared per-species textures, removes hero emissive boost, reduces school instance tinting to near-neutral for asset-backed meshes, and keeps procedural canvas generation as an emergency-only fallback with more fish-like striping / dorsal-belly contrast. Added tests in `DetailedFish.test.ts` for preserve-first map behavior, species texture usage, no-emissive hero materials, and subdued school instancing tint; added tests in `visualAssets.test.ts` locking the fish atlas manifest. Added reproducible generator `scripts/generate-fish-assets.mjs` that emits per-species PNG atlases plus refreshed GLBs: all school GLBs are single-mesh/single-material with embedded baseColor+normal+metallicRoughness textures; all hero GLBs are 2-mesh body/fin assets where the fin material is alpha-blended and double-sided. Verification passed: `npm run test`, `npm run typecheck`, `npm run build`. Final material routing: tropical/angelfish/neon/goldfish all prefer embedded GLB materials for school and hero; external PNG atlases exist as fallback supplements if a GLB material channel is missing; procedural fallback is only for asset-missing cases.