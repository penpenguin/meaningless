2026-03-21: Reworked `nature-showcase` substrate transition to remove the white central-right pedestal feel. In `src/components/Aquascaping.ts`, `createHardscapeTransitionDetails()` now skips `hardscape-transition-berm` for `nature-showcase` and instead adds low irregular `hardscape-transition-detritus`, expanded `hardscape-transition-pebble`, and `hardscape-partial-burial` meshes localized from the left mound into the center transition while preserving the right-front beach. `createSandDetails()` also reduces `nature-showcase` ripple count, size, and opacity so the pale sand no longer reads as a bright stage streak. In `src/components/AdvancedScene.ts`, `sampleSubstrateHeight()` now strengthens the left-front dark mound shoulder, narrows and pushes the pale beach farther right-front, and adds asymmetric lift/dip terms so the mound-to-beach boundary reads as an irregular shoreline instead of a smooth oval pedestal. `createSubstrate()` darkens and lowers opacity for `nature-showcase` top/front sediment detail layers and slightly lowers front sand brightness to suppress white streaking. Regression coverage in `src/components/Aquascaping.test.ts` asserts no showcase berm and requires detritus/burial scatter; `src/components/AdvancedScene.test.ts` now checks the old center-right pedestal sample sits between left mound and right-front beach and that shoreline samples contain irregular rises/dips. Verified with `npm run test` and `npm run typecheck`.