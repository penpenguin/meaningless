Made the aquarium visuals calmer and more aquarium-like. Updated render model water tints to deeper teal tones (#0b5666 healthy, #0a4b5a mid, #21424d dirty). Enhanced `applyGradientBackground` with radial surface bloom and lower vignette while preserving compatibility with minimal mocked 2D contexts. Tuned `AdvancedAquariumScene` camera to `position.set(0, 1.1, 14.5)` and `lookAt(0, -1.4, 0)` for a closer downward framing. Muted substrate palette from bright white to warm sand tones (`0xB59E84` base, `0xD2BB9B` sand) and enriched the procedural sand texture with darker grains and edge shading. Added aquarium-shell reflection layers via `#canvas-container::before/::after` and increased HUD glassmorphism transparency/blur. Added/updated tests in `src/game/renderModel.test.ts`, `src/__tests__/gradientBackground.test.ts`, `src/components/AdvancedScene.test.ts`, and `src/__tests__/viewportLayout.test.ts`. Verified with `npm run test`, `npm run typecheck`, `npm run lint`, plus Playwright screenshots at 1600x900 and 390x844 showing no page-level scroll.