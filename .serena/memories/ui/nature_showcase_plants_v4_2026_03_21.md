2026-03-21: Updated `src/components/Aquascaping.ts` nature-showcase planting to v4. `natureShowcasePlantClusterDefinitions` now avoid a full vallis curtain: `left-rear` is a localized tall backdrop with stem/hygrophila dominant and vallis minor, `left-mid-broadleaf` and `center-left-fern-mass` create broadleaf shoulder + driftwood-attached fern masses, `right-mid-crypt` and `right-rear` use crypt-led accents with small hygrophila/anubias support, and `left-foot` remains the only sparse foreground tuft so the right-front sand stays open. `createHeroCanopy()` for nature-showcase now uses four localized masses (vallis left-rear, stem left-rear shoulder, java fern center-left, crypt right-rear) instead of a taller curtain-like run. `natureShowcaseHardscapePlantAnchors` was expanded from 12 to 22 anchors across branch crotches, root flares, fork junctions, and rock crevices so anubias/java fern read as attached to wood and rock. `getPlantTint()` background lightness floors were lifted for vallis, stem/hygrophila, and crypt so backdrop plants do not read as black lines. Added/updated Vitest coverage in `src/components/Aquascaping.test.ts` for species layering, localized canopy, expanded hardscape anchors, and nature-showcase background tint floor. Verified with `npm run test` and `npm run typecheck`.