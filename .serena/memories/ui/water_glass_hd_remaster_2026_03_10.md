2026-03-10: Implemented first HD remaster pass for water and glass in `AdvancedAquariumScene` using TDD. Added failing tests in `src/components/AdvancedScene.test.ts` to require premium high-quality highlight layers (`tank-glass-front-highlight`, `tank-water-surface-highlight`) and richer refractive material settings, plus low-quality suppression of those premium layers. Production changes: added front glass highlight mesh and water surface highlight mesh, richer physical material defaults for glass/water (attenuation distance/color, thickness, ior, specular intensity), procedural highlight textures for both layers, subtle motion offsets for highlight maps, and quality-specific material tuning inside `applyVisualQuality`. `applyTheme` now reapplies `applyVisualQuality(this.currentVisualQuality)` after theme updates so quality-specific tuning remains active. Verified with `npm run test`, `npm run typecheck`, and `npm run lint`.