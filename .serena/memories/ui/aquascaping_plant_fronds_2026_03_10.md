2026-03-10: Reworked `AquascapingSystem` plant rendering away from stacked 6-sided cylinders into curved `PlaneGeometry` ribbon fronds. Each plant cluster now builds 4-6 fronds with a shared leaf material per plant, using new cutout seaweed color/normal/roughness canvas textures to avoid noisy per-segment randomness and improve silhouette readability underwater. Added a regression test in `src/components/Aquascaping.test.ts` that fails if foreground seaweed falls back to cylinder geometry. Verified with `npm run test`, `npm run typecheck`, `npm run lint`, and local Playwright screenshots at `http://127.0.0.1:4173/meaningless/`.