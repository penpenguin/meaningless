2026-03-20: Reworked `src/components/Aquascaping.ts` planted freshwater layout away from generic sword/fan curtain foliage toward species-driven zones and hardscape-attached broadleaf patches. `PlantType` now includes concrete species (`javafern-large`, `javafern-narrow`, `anubias-nana-clump`, `anubias-petite-clump`, `crypt-brown`, `stem-green-bush`, `vallisneria-tall`, `hygrophila-rear`) while preserving generic legacy types for marine/procedural paths. `plantClusterDefinitions` now emphasize left-rear vallis/stem backdrop, left-mid + center-mid java fern/anubias masses, right-mid/right-rear crypt accents, and a sparse left-side foreground petite anubias zone so the right-front sand stays open. Sampled plant placements are no longer consumed from module-scope fixed arrays; use `resolveSampledPlantPlacements(layoutStyle, seed)` and `resolveSubstratePlantAnchors(layoutStyle, seed)` to regenerate deterministic layouts from a seed each call. Added `resolveHardscapePlantAnchors(layoutStyle)` with driftwood branch-crotch/root-flare and rock-crevice anchors; `createHeroDriftwood`/`createHeroRockRidge` now attach epiphyte groups there. `src/assets/visualAssets.ts` now registers new plant GLBs and `public/assets/aquarium/` contains generated GLBs for the new species. `DetailedFishSystem` now resolves substrate anchors by layout style instead of importing fixed planted arrays; `AdvancedScene` passes the current theme layout style into fish construction. Verified with `npm test`, `npm run typecheck`, and `npm run build`.