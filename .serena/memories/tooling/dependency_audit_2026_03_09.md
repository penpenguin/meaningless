2026-03-09: package dependency cleanup removed unused devDependency `playwright`. `@types/three` was investigated as a removal candidate but is still required for `npm run typecheck` because this codebase relies on external Three.js type declarations. Security remediation updated `@typescript-eslint/eslint-plugin` and `@typescript-eslint/parser` to 8.56.1, which cleared npm audit high-severity findings to zero. Added tooling test coverage to keep Playwright removed and keep @typescript-eslint on major 8 or newer.