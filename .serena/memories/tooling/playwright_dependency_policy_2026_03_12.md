2026-03-12: Repository policy changed to keep `playwright` in top-level devDependencies because Codex skills (notably playwright-interactive) rely on it being available in-workspace. The previous `src/__tests__/dependencyHygiene.test.ts` assertion that `package.json.devDependencies.playwright` must be undefined was removed as it contradicted the desired setup. Future hygiene checks should not enforce removing Playwright from devDependencies unless project policy changes again.