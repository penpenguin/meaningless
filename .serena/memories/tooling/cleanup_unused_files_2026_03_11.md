2026-03-11: Cleanup removed unused devDependency `playwright` from package.json/package-lock.json and deleted `scripts/underwater-loop.d.ts` by moving its types into `scripts/underwater-loop.js` via JSDoc. `tsconfig.json` now enables allowJs/checkJs so the JS generator script remains type-checked from TS tests. Validation passed with `npm run test`, `npm run typecheck`, `npm run lint`, and `npm run build`.