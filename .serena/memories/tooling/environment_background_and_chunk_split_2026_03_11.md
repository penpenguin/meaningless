2026-03-11: Follow-up cleanup after aquarium visual upgrade. First, removed `EnvironmentLoader` ownership of `scene.background` so env loading no longer gets overwritten during `AdvancedScene` init. Added exported `createEnvironmentBackdropTexture()` in `src/components/Environment.ts` and reused it from `applyGradientBackground()` so runtime background and reflection/backdrop cues now share the same depth motif. Added regression coverage in `src/components/Environment.test.ts` (shared backdrop texture marker + background preservation) and `src/__tests__/gradientBackground.test.ts` (`isEnvironmentBackdrop`). Second, improved build chunking diagnostics with a node-environment test in `src/__tests__/viteConfig.test.ts` and changed `vite.config.ts` `manualChunks` to split `three-addons`, `three-core`, and `lottie`. Also hardened `src/__tests__/setup.ts` so node-environment tests do not crash when `HTMLCanvasElement`/`window` are absent. Verification: `npm run test`, `npm run typecheck`, `npm run build`. Result: runtime background ownership issue is fixed; build still warns for `three-core` (>500 kB because `three.module.js` is a single upstream module) and `lottie-web` eval remains, so further reduction would require either accepting the three core size warning or replacing/lazy-loading the lottie dependency.