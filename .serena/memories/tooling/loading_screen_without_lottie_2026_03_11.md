2026-03-11: Replaced `lottie-web` loading overlay with procedural DOM/CSS bubbles to remove the build-time eval warning and drop the dependency. Added TDD coverage in `src/utils/loadingScreen.test.ts` for rendering seven `.loading-bubble` nodes plus `.loading-bubble-core`, and idempotent reinitialization. Rewrote `src/utils/loadingScreen.ts` to generate a bubble cluster from static specs, updated `src/styles.css` with `loading-bubbles` / `loading-bubble-*` styles and keyframes, removed obsolete `vi.mock('lottie-web')` blocks from app startup tests, simplified `vite.config.ts` chunking (only `three-core` and `three-addons`), and removed `lottie-web` from `package.json`/`package-lock.json` via `npm uninstall lottie-web`. Verified with targeted vitest, full `npm run test`, `npm run typecheck`, and `npm run build`. Result: `lottie-web` eval warning is gone; only the upstream `three-core` >500 kB chunk warning remains.